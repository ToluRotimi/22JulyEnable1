# resource "aws_security_group"{
   # name = "SSH Access"
   # description = Enable SSH access on 0.0.0.0/0"
   # vpc_id = aws_vpc.vpc.id
# }

/* ingress {
    from_port = 22
    to_port
    protocol
    cidr_blocks
} */
